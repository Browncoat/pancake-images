## Pancake Images

# Just a folder of panckage images to run a static file server from. 

Run the following command in Terminal in the the cloned folder to start a python static file server.

`python3 -m http.server 8000`
